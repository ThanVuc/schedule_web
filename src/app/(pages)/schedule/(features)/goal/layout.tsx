
export const metadata = {
    title: "Quản Lý Mục Tiêu | Schedulr",
    description: "Xem và quản mục tiêu của bạn",
};

const GoalLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>{children}</>
    );
}

export default GoalLayout;