
export const metadata = {
    title: "Lịch Trình Hằng Ngày | Schedulr",
    description: "Xem và quản lý lịch trình hằng ngày của bạn",
};

const DailyLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>{children}</>
    );
}

export default DailyLayout;