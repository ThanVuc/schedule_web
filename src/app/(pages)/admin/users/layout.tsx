
export const metadata = {
    title: "Người Dùng | Schedulr",
    description: "Quản lý người dùng và cài đặt hệ thống",
}

const UserLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
        </>
    );
};

export default UserLayout;
