
export const metadata = {
    title: "Quyền Hạn | Schedulr",
    description: "Quản lý quyền hạn trong hệ thống",
}

const PermissionsLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
        </>
    );
};

export default PermissionsLayout;