
export const metadata = {
    title: "Vai Trò | Schedulr",
    description: "Quản lý vai trò trong hệ thống",
}

const RolesLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
        </>
    );
};

export default RolesLayout;
